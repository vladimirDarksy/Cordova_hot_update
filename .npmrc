# pnpm compatibility settings
# These settings help ensure the plugin works correctly when installed via pnpm

# Allow automatic installation of peer dependencies
auto-install-peers=true

# Hoist Cordova plugins to avoid issues with Cordova CLI
public-hoist-pattern[]=cordova-plugin-*

# Ensure this plugin is hoisted
public-hoist-pattern[]=cordova-plugin-hot-updates

# Shamefully hoist to resolve potential dependency issues
shamefully-hoist=false

# Note: If you encounter issues with Cordova CLI not finding the plugin,
# try setting shamefully-hoist=true in your project's .npmrc
